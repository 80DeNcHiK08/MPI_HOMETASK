#include "pch.h"
#include "Tree.h"
#include "Tree.cpp"
#include "Student.h"
#include "Group.h"
#include "Faculty.h"
#include <ctime>

using namespace std;

int main()
{
	/*Tree<int, int> tree;
	//int counter = 1;
	for (int i = 1; i <= 32; i++)
	{
		/*clock_t start = clock();
		double duration;*/
		//tree.Add(rand());
		/*duration = ((double)clock() - start) / (double)CLOCKS_PER_SEC;
		string s;
		int scase = counter % 10;
		switch (scase)
		{
		case 1: s = "st"; break;
		case 2: s = "nd"; break;
		case 3: s = "rd"; break;
		default: s = "th"; break;
		}
		cout << "Adding " << counter << "'" << s << " element; Time: " << duration << endl;
		counter++;*/
	/*}

	tree.LevelOrderPrint();

	int returned;
	if (tree.Find(21, returned, true))
	{
		cout << "returned: " << returned << endl;
	}
	else
	{
		cout << "There is no element with such key!" << endl;
	}

	tree.Remove(21);

	if (tree.Find(21, returned, false))
	{
		cout << "returned: " << returned << endl;
	}
	else
	{
		cout << "There is no element with such key!" << endl;
	}

	tree.LevelOrderPrint();*/

	/*Group KS = Group();
	Faculty FFEKS = Faculty();
	FFEKS.PrintAllInfo();
	Student stud1 = Student();
	Student stud2 = Student();
	stud1 = stud2;
	KS.Add(stud1);
	KS.Add(stud2);
	KS.Add(stud2);
	KS.Add(stud2);
	FFEKS.Add(KS);

	KS.PrintAllInfo();
	KS.Remove(0);*/
 }